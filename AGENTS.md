# Project Rules & Guidelines

## 1. UI Layout Verification & Recursive Self-Correction (UI 레이아웃 자가 검증 및 재귀 수정 원칙)
- **자율적 시각 검증**: UI 컴포넌트를 추가/수정하거나 테마 미리보기 이미지(스크린샷)를 생성할 때, 에이전트는 생성된 이미지를 직접 확인(`view_file`)하여 텍스트 잘림, 컨트롤 간 겹침/충돌, 여백 부족 등 **UI 간 문구 간섭을 스스로 판단하고 재귀적으로 수정**해야 한다.
- **다국어 및 다중 테마 전수 검증**:
  - 한국어와 영어 양쪽 언어(KOR/ENG) 환경에서 단어 길이 차이로 인한 레이아웃 깨짐이 없는지 전수 검증한다.
  - 다크 모드와 라이트 모드 각각에서 컨트롤의 배경색, 전경 글자색, 테두리 대비가 시인성을 해치지 않는지 확인한다.
- **안전 여백 확보 및 공간 확장**:
  - 인접 컨트롤 간 및 버튼과의 사이에 안전 여백을 넉넉히 확보하며, 공간이 좁을 경우 폼 너비/높이를 적절히 확장하여 시각적 완성도를 높인다.
- **자가 개선 루프**:
  - 사용자가 지적하기 전에 에이전트가 자체적으로 문제를 발견하고, 코드 수정 -> 재빌드 -> 스크린샷 갱신 및 재확인 루프를 완수한 뒤 최종 보고한다.

## 2. General Project Rules
- **가독성 최우선**: 변수명은 직관적으로, 로직은 단순하게 작성할 것.
- **주석 필수**: 코드가 무엇을 하는지보다, 왜 이렇게 짰는지 의도를 설명할 것.
- **모듈화**: 하나의 파일이 너무 길어지지 않게 기능별로 분리할 것.
- **에러처리**: 예외 상황을 항상 고려하여 코드를 작성할 것.
- **두괄식 답변**: [결론] -> [상세 설명] 순서로 작성할 것.
- **빌드 완료 시 알림음**: 최종 빌드 완료 후 딩동 사운드를 1회 재생하고 GitHub에 커밋 및 푸시할 것.

## 3. GitHub Preview Assets Rule (깃헙 미리보기 영문 게시 원칙)
- **미리보기 이미지 영문 게시 필수**: GitHub 리포지토리(README.md, Release 페이지, Wiki 등)에 공개 게시되는 모든 기본 미리보기 이미지(`assets/screenshot.png`, `assets/screenshot_dark.png`, `assets/screenshot_light.png`)는 **항상 영문(English) 버전**으로 생성 및 게시해야 한다.
- 한국어 스크린샷은 다국어 검증용 보조 에셋(`screenshot_ko_dark.png`, `screenshot_ko_light.png`)으로 분리 관리한다.
